# Cheri Cheri pwn docker

Per lanciare la challenge è consigliabile usare compose (per il run, non per compile e debug).

Gli altri target usano il `.sh` interno, anche il run ha un suo `.sh` interno che è possibile usare per debug senza compose.
